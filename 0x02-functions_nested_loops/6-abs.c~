#include "main.h"

/**
 * _abs - Compute the absolute value of an integer
 *
 * @n: reps. integers
 *
 * Return: Absolute value of integer
 */
int _abs(int n)
{
	int n;

	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
